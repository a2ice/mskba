# Tasks

Список задач проекта. Каждая задача ведется отдельной папкой в `docs/tasks/{NNN}`.

Формат строки:

```markdown
- [ ] NNN - Название или краткое описание задачи
```

Статусы:

- `[ ]` - задача запланирована или ожидает старта.
- `[x]` - задача выполнена, закоммичена и вмержена в `main`.

## Список задач

- [x] 001 - Тестирование task workflow - метод `AuthController@restore` должен возвращать текст "Восстановление пароля пока не реализовано. Сорян, но мы работаем над этим! Прямо сейчас!!!"
- [x] 002 - Рефакторинг системных и пользовательских ролей: `UserRole` -> `UserSystemRole`, nullable `system_role`, модель `UserParticipationRole`
- [x] 003 - Привести Docker-инфраструктуру проекта к единой dev/prod схеме
- [x] 004 - Добавить сущность `UserProfile` в контексте `Identity` со связью `User` 1-to-1 и автосозданием при регистрации
- [x] 005 - Доработать главный экран главной страницы по новому макету с блоками `Сейчас играют` и `Highlights`
