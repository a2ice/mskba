# Task 129 follow-up — tooltip icons and team background alpha

- Icon-only tooltip sources must use the title/floating tooltip directly: no generated `?` trigger and no dotted underline.
- Text tooltip sources keep the existing text affordances.
- Team settings reset controls are explicitly marked as icon tooltip sources.
- Mirrored team background gradient keeps the `primary → secondary → primary` shape and uses alpha `0.65`.
