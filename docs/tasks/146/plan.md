# 146 — План

1. [x] Восстановить актуальный `main` после Task 145 и подтвердить причину узкого layout во вложенном popup.
2. [x] Зафиксировать UX-ожидание по приоритету predictive-поиска метро.
3. [x] Исправить полноширинный одноколоночный layout active mini-wizard во вложенном modal.
4. [x] Добавить приоритет `startsWith(query)` для видимых predictive-вариантов метро с алфавитной сортировкой внутри групп.
5. [x] Подключить regression-fix к frontend bundle без изменения доменной логики поиска.
6. [ ] Проверить diff и CI (`php artisan test` + frontend build).
7. [ ] При зелёном CI вмержить PR в `main` по заранее данному разрешению пользователя.
