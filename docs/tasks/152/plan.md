# 152 — План

1. [x] Проверить stacking context preview площадки и сравнить его с исправлением map modal из Task 150.
2. [x] Поднять только venue preview modal выше вложенных wizard-слоёв.
3. [x] Добавить в пустую выдачу поиска мероприятий CTA на вкладку `Создать`.
4. [ ] Проверить CI (`php artisan test` + frontend build).
5. [ ] При зелёном CI вмержить PR в `main` по текущему fast follow-up workflow.
