# 150 — План

1. [x] Проверить stacking context базового modal, location mini-wizard и venue map.
2. [x] Поднять только modal карты выше обоих wizard-слоёв.
3. [ ] Прогнать CI (`php artisan test` + frontend build).
4. [ ] При зелёном CI сразу вмержить PR в `main` по прямому разрешению пользователя.
