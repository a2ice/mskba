# 149 — План

1. [x] Проверить актуальный `main` и существующий общий venue selector.
2. [x] Подтвердить, что карта уже использует тот же `searchUrl` и dataset-фильтры, что predictive-поиск.
3. [x] Вернуть ссылку `На карте` на финальном шаге location mini-wizard.
4. [x] Не дублировать карту и не менять существующий `venue_id` flow.
5. [ ] Проверить diff и CI (`php artisan test` + frontend build).
6. [ ] При зелёном CI сразу вмержить PR в `main` по прямому разрешению пользователя.
