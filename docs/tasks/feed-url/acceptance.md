# Acceptance criteria

Изменение считается готовым, если canonical `/feed` routes и legacy 301 redirects проходят feature tests и route cache validation. Merge в `main` выполняется отдельно после ручной проверки.
